# 🤖 WebBeds Reference Automation - Chrome Extension

إضافة Chrome لأتمتة إضافة مراجع WebBeds مباشرة من المتصفح.

## 🚀 التثبيت

### 1. تحميل الإضافة
```bash
# انسخ مجلد chrome-extension إلى مكان آمن
```

### 2. تثبيت في Chrome
1. افتح Chrome واذهب إلى `chrome://extensions/`
2. فعل "Developer mode" في الزاوية اليمنى العلوية
3. اضغط "Load unpacked"
4. اختر مجلد `chrome-extension`
5. ستظهر الإضافة في شريط الأدوات

## 📋 كيفية الاستخدام

### الخطوة 1: تحضير الملف
- تأكد من أن ملف Excel يحتوي على:
  - `WebBeds Booking Number` (مثل: HTL-WBD-846646915)
  - `Supplier reference` (للتحقق من الحالة)
  - `ClientReference` (رقم الحجز)
  - `HotelConf` (رقم المرجع المطلوب إضافته)

### الخطوة 2: استخدام الإضافة
1. **انتقل إلى WebBeds**: `https://extranet.webbeds.com/5520905/bookings`
2. **اضغط على أيقونة الإضافة** في شريط الأدوات
3. **ارفع ملف Excel**: اسحب الملف أو اضغط لاختياره
4. **اضغط "تحليل الملف"**: سيتم استخراج الحجوزات التي تحتاج مراجع
5. **اضغط "بدء الأتمتة"**: ستبدأ العملية تلقائياً

### الخطوة 3: مراقبة التقدم
- شريط التقدم يظهر الحالة الحالية
- رسائل الحالة تظهر تفاصيل كل خطوة
- النتائج النهائية تظهر عدد النجاح/الفشل

### الخطوة 4: تحميل التقرير
- اضغط "تحميل التقرير" للحصول على ملف CSV
- يحتوي على تفاصيل كل حجز ونتيجة المعالجة

## 🔧 آلية العمل

### 1. معالجة الملف
```javascript
// استخراج رقم الحجز من WebBeds Booking Number
HTL-WBD-846646915 → 846646915

// التحقق من Supplier Reference
if (empty || not_number) {
    needsReference = true;
}
```

### 2. الأتمتة
```javascript
// لكل حجز يحتاج مرجع:
1. اختيار "All Hotels"
2. البحث عن رقم الحجز
3. النقر على "Add Reference"
4. إدخال HotelConf
5. حفظ التغييرات
```

### 3. التقارير
- نتائج فورية في الإضافة
- تقرير CSV قابل للتحميل
- تسجيل الأخطاء والنجاحات

## 📁 هيكل الملفات

```
chrome-extension/
├── manifest.json          # إعدادات الإضافة
├── popup.html            # واجهة المستخدم
├── popup.js              # منطق الواجهة
├── content.js            # التفاعل مع WebBeds
├── background.js         # خدمات الخلفية
└── README.md            # التعليمات
```

## ⚠️ ملاحظات مهمة

1. **الأمان**: 
   - الإضافة تعمل فقط على موقع WebBeds
   - لا تحفظ بيانات تسجيل الدخول

2. **الأداء**:
   - تأخير 3 ثوان بين كل حجز
   - معالجة تدريجية لتجنب الحظر

3. **المتطلبات**:
   - تسجيل دخول مسبق إلى WebBeds
   - ملف Excel بالتنسيق الصحيح

## 🐛 حل المشاكل

### الإضافة لا تعمل:
- تأكد من تفعيل Developer mode
- أعد تحميل الإضافة
- تحقق من وحدة التحكم للأخطاء

### فشل في البحث:
- تأكد من صحة أرقام الحجز
- تحقق من اتصال الإنترنت
- تأكد من تسجيل الدخول إلى WebBeds

### خطأ في إضافة المرجع:
- تحقق من وجود الحجز
- تأكد من عدم وجود مرجع مسبقاً
- راجع صلاحيات المستخدم

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى مراجعة وحدة التحكم في المتصفح أو التواصل مع فريق التطوير.