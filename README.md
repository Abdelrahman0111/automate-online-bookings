# 🤖 أتمتة إضافة مراجع WebBeds

تطبيق Streamlit لأتمتة إضافة Supplier Reference في موقع WebBeds باستخدام بيانات من ملفات Excel.

## 🎯 الهدف

- مقارنة ملف WebBeds مع ملف جود (arrivals_jood_webbeds)
- استخراج الحجوزات التي تحتاج إضافة Supplier Reference
- أتمتة عملية إضافة المراجع في موقع WebBeds

## 📋 المتطلبات

### البرامج المطلوبة:
- Python 3.8+
- Google Chrome Browser
- ChromeDriver

### المكتبات المطلوبة:
```bash
pip install -r requirements.txt
```

## 🚀 التثبيت والإعداد

### 1. تحميل المشروع
```bash
git clone [repository-url]
cd Automation_add_supplier_number_on_webbeds_from_jood
```

### 2. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 3. إعداد ChromeDriver
```bash
python setup_chrome.py
```

### 4. تشغيل التطبيق
```bash
streamlit run app.py
```

## 📊 كيفية الاستخدام

### الخطوة 1: رفع الملفات
1. **ملف WebBeds**: يحتوي على:
   - `WebBeds Booking Number` (مثل: HTL-WBD-846646915)
   - `Supplier reference`

2. **ملف جود**: يحتوي على:
   - `ClientReference` (رقم الحجز)
   - `HotelConf` (رقم تأكيد الفندق)

### الخطوة 2: المقارنة
- اضغط على "مقارنة الملفات"
- سيتم استخراج الأرقام من WebBeds Booking Number
- مطابقة مع ClientReference في ملف جود
- التحقق من وجود Supplier Reference صحيح

### الخطوة 3: الأتمتة
1. أدخل بيانات تسجيل الدخول لـ WebBeds
2. اضغط على "بدء الأتمتة"
3. سيقوم التطبيق بـ:
   - تسجيل الدخول تلقائياً
   - البحث عن كل حجز
   - إضافة HotelConf كـ Supplier Reference
   - تقرير مفصل عن النتائج

## 🔧 آلية العمل

### استخراج رقم الحجز:
```
HTL-WBD-846646915 → 846646915
```

### المطابقة:
- `WebBeds Booking Number` (بعد استخراج الرقم) = `ClientReference`
- إذا تطابق: يتحقق من وجود `Supplier Reference`
- إذا فارغ أو غير صحيح: يضاف `HotelConf`

### الأتمتة:
1. الانتقال إلى: `https://extranet.webbeds.com/5520905/bookings`
2. اختيار "All Hotels"
3. البحث عن رقم الحجز
4. النقر على "Add Reference"
5. إدخال `HotelConf`
6. حفظ التغييرات

## 📁 هيكل الملفات

```
├── app.py                 # التطبيق الرئيسي
├── requirements.txt       # المكتبات المطلوبة
├── setup_chrome.py       # إعداد ChromeDriver
├── README.md             # التعليمات
└── chromedriver.exe      # (يتم تحميله تلقائياً)
```

## 📊 التقارير

### تقرير المقارنة:
- إجمالي الحجوزات
- الحجوزات المطابقة
- يحتاج إضافة مرجع
- مكتمل

### تقرير الأتمتة:
- الحجوزات المعالجة بنجاح
- الحجوزات الفاشلة
- أسباب الفشل
- الوقت المستغرق

## ⚠️ ملاحظات مهمة

1. **الأمان**: لا تشارك بيانات تسجيل الدخول
2. **السرعة**: يوجد تأخير بين الطلبات لتجنب الحظر
3. **الاستقرار**: تأكد من اتصال إنترنت مستقر
4. **المتصفح**: لا تغلق المتصفح أثناء العمل

## 🐛 حل المشاكل

### خطأ ChromeDriver:
```bash
python setup_chrome.py
```

### خطأ تسجيل الدخول:
- تحقق من بيانات الدخول
- تأكد من عدم وجود 2FA مفعل

### خطأ في البحث:
- تحقق من صحة أرقام الحجز
- تأكد من وجود الحجز في النظام

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.